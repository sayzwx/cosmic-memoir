export const CRYSTAL_VERTEX = `uniform float uTime,uEnergy,uFocus,uCapture,uDetail;varying vec3 vNormal,vView,vLocal,vWorldNormal;varying float vFacet;void main(){  vec3 p=position;  float shard=sin(dot(p,vec3(7.7,12.9,5.3))+uTime*1.15);  p+=normal*shard*(.003+uEnergy*.009)*uDetail;  vec4 mv=modelViewMatrix*vec4(p,1.0);  vNormal=normalize(normalMatrix*normal);vWorldNormal=normalize(mat3(modelMatrix)*normal);  vView=normalize(-mv.xyz);vLocal=position;vFacet=shard;  gl_Position=projectionMatrix*mv;}`;export const CRYSTAL_FRAGMENT = `uniform vec3 uColor,uHotColor;uniform float uTime,uEnergy,uFocus,uCapture,uOpacity,uVariant,uDetail;varying vec3 vNormal,vView,vLocal,vWorldNormal;varying float vFacet;float thinLine(float value,float width){  float d=abs(fract(value)-.5);return 1.0-smoothstep(width,width+fwidth(value)*1.15,d);}void main(){  float facing=clamp(abs(dot(normalize(vNormal),normalize(vView))),0.0,1.0);  float fresnel=pow(1.0-facing,2.15);  vec3 cell=vLocal*vec3(3.7,4.9,3.3);  float facet=min(min(abs(fract(cell.x)-.5),abs(fract(cell.y)-.5)),abs(fract(cell.z)-.5));  float facetEdge=1.0-smoothstep(.455-fwidth(facet)*1.5,.455+fwidth(facet)*1.5,facet);  float pathA=thinLine(dot(vLocal,vec3(3.1,-2.4,1.7))-uTime*.055,.018);  float pathB=thinLine(dot(vLocal,vec3(-1.3,2.7,2.1))+uTime*.035,.012);  float paths=max(pathA,pathB)*uDetail;  float energy=clamp(uEnergy+uFocus*.42+uCapture*.25,0.0,1.35);  float caustic=0.5+0.5*sin(dot(vLocal,vec3(5.1,-3.7,6.4))+uTime*.72);  float internal=pow(max(0.0,1.0-length(vLocal)*.52),1.8)*(.55+.45*caustic);  vec3 spectral=0.5+0.5*cos(6.28318*(fresnel*.72+uTime*.018+vec3(0.0,.19,.38)));  vec3 base=mix(uColor,uHotColor,clamp(fresnel*.52+facetEdge*.18+paths*.3+energy*.16,0.0,.9));  base=mix(base,base*.65+spectral*.35,clamp(fresnel*.42+facetEdge*.16,0.0,.6));  base+=uHotColor*internal*.18;  float alpha=uOpacity*(.36+fresnel*.52+facetEdge*.16+paths*.2+energy*.1);  if(uVariant>.5&&uVariant<1.5){    float lens=pow(fresnel,1.4);base=mix(base,uHotColor,lens*.42);alpha*=.86+lens*.22;  }  if(uVariant>1.5&&uVariant<2.5){    float crack=max(thinLine(dot(vLocal,vec3(2.7,5.1,-3.3))+sin(vLocal.z*9.0)*.22,.014),                    thinLine(dot(vLocal,vec3(-4.3,1.9,3.7))+sin(vLocal.x*11.0)*.18,.011));    vec3 gold=vec3(1.0,.55,.12);base=mix(base,gold,crack*.92);alpha+=crack*.22;  }  if(uVariant>2.5&&uVariant<3.5){    float rim=pow(1.0-facing,1.25);base=mix(base,uHotColor,rim*.64);alpha+=rim*.14;  }  if(uVariant>3.5){    float eventRim=pow(1.0-facing,5.5);    base=vec3(.0005,.0007,.0015)+uHotColor*eventRim*.055;    alpha=min(1.0,uOpacity*(.94+eventRim*.06));  }  gl_FragColor=vec4(max(base*(.66+energy*.2+vFacet*.012),vec3(0.0)),clamp(alpha,0.0,1.0));}`;export const PHOTO_VERTEX = `uniform float uUnfold,uCapture,uAnchor;varying vec2 vUv;void main(){  vUv=uv;vec3 p=position;float reveal=clamp(uUnfold,0.0,1.0);  p.x*=reveal;p.z+=(1.0-reveal)*abs(uv.x-.5)*.32-uCapture*.08;p.y+=uAnchor*.04;  gl_Position=projectionMatrix*modelViewMatrix*vec4(p,1.0);}`;export const PHOTO_FRAGMENT = `uniform sampler2D uMap;uniform float uUnfold,uOpacity,uFocus,uPixelRatio;varying vec2 vUv;void main(){  float reveal=step(abs(vUv.x-.5)*2.0,uUnfold+.001);  if(reveal<.5)discard;  vec4 image=texture2D(uMap,vUv);  vec2 edge=min(vUv,1.0-vUv);vec2 pixel=max(fwidth(vUv),vec2(.000001));  float edgePixels=min(edge.x/pixel.x,edge.y/pixel.y);  float frame=1.0-smoothstep(1.0-.25/uPixelRatio,1.0+.25/uPixelRatio,edgePixels);  if(image.a<.01&&frame<.5)discard;  vec3 energy=vec3(.40,.88,1.0);  float bevel=1.0-smoothstep(1.0,4.0,edgePixels);  vec3 spectral=.5+.5*cos(6.28318*(edgePixels*.018+vec3(0.0,.2,.42)));  vec3 color=image.rgb*(1.0+min(uFocus*.12,.12));  color=mix(color,energy,frame);  color+=spectral*bevel*.12*uFocus;  gl_FragColor=vec4(color,max(image.a,frame+bevel*.08)*uOpacity);}`;export const ENERGY_VERTEX = `uniform float uTime,uEnergy;varying vec3 vPosition,vNormal,vView;void main(){  vPosition=position;vec3 p=position*(1.0+sin(uTime*1.45+dot(position,vec3(3.0,7.0,5.0)))*uEnergy*.009);  vec4 mv=modelViewMatrix*vec4(p,1.0);vNormal=normalize(normalMatrix*normal);vView=normalize(-mv.xyz);  gl_Position=projectionMatrix*mv;}`;export const ENERGY_FRAGMENT = `uniform float uTime,uEnergy,uFocus,uDetail;uniform vec3 uColor;varying vec3 vPosition,vNormal,vView;float wire(float value,float width){float d=abs(fract(value)-.5);return 1.0-smoothstep(width,width+fwidth(value),d);}void main(){  float a=wire(dot(vPosition,vec3(2.5,4.2,-1.7))-uTime*.12,.012);  float b=wire(dot(vPosition,vec3(-3.3,1.4,2.8))+uTime*.075,.009)*uDetail;  float rim=pow(1.0-abs(dot(normalize(vNormal),normalize(vView))),3.0);  float path=max(a,b);float alpha=(path*.13+rim*.026)*(uEnergy+uFocus*.4);  if(alpha<.006)discard;gl_FragColor=vec4(uColor*(.72+path*.32),min(alpha,.28));}`;