import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js';import { getQualityBudget, MAX_ENVIRONMENT_BUDGET } from './qualityBudgets.js';import { createRandom, disposeRenderable, setPosition } from './math.js';const RIBBON_VERTEX = `attribute vec3 aStart,aEnd;attribute float aSide,aAlong,aPath,aWidth;uniform float uTime,uBreath,uResonance,uPixelRatio;uniform vec2 uViewport;varying float vAcross,vPath,vAlong;vec3 animatePoint(vec3 p,float phase){  float breath=1.0+uBreath*.03*(.5+.5*sin(uTime*.785398163));  float wave=sin(uTime*1.45+phase*19.0);  return p*breath+normalize(p+vec3(.001))*uResonance*wave*.42;}void main(){  vec3 start=animatePoint(aStart,aPath),end=animatePoint(aEnd,aPath+.17);  vec4 va=modelViewMatrix*vec4(start,1.),vb=modelViewMatrix*vec4(end,1.);  vec4 clipA=projectionMatrix*va,clipB=projectionMatrix*vb;  vec2 screenDelta=(clipB.xy/clipB.w-clipA.xy/clipA.w)*uViewport;  vec2 normal=normalize(vec2(-screenDelta.y,screenDelta.x)+vec2(.0001,0.));  vec4 clip=mix(clipA,clipB,aAlong);  float taper=.34+.66*sin(3.14159265*aAlong);  float width=aWidth*(1.0+uResonance*.65)*taper*uPixelRatio;  clip.xy+=normal*aSide*width*2.0/uViewport*clip.w;  gl_Position=clip;vAcross=aSide;vPath=aPath;vAlong=aAlong;}`;const RIBBON_FRAGMENT = `uniform float uOpacity,uResonance;varying float vAcross,vPath,vAlong;void main(){  float aa=max(fwidth(vAcross)*.72,.018);float edge=1.0-smoothstep(1.0-aa,1.0,abs(vAcross));  float current=.5+.5*sin(vPath*83.0+vAlong*8.0);   vec3 blue=vec3(.08,.36,.68),cyan=vec3(.36,.86,.94),pink=vec3(.76,.32,.67);   vec3 color=mix(blue,cyan,.38+.62*current);   color=mix(color,pink,smoothstep(.74,.98,fract(vPath*7.0))*.38)+uResonance*vec3(.12,.10,.04);  float alpha=uOpacity*edge*(.48+.30*current+.20*uResonance);  if(alpha<.012)discard;gl_FragColor=vec4(color,alpha);}`;const FLOW_VERTEX = `attribute vec3 aStart,aEnd;attribute float aPhase;uniform float uTime,uResonance,uPixelRatio;varying float vPower;void main(){float t=fract(aPhase+uTime*(.045+uResonance*.09));vec3 p=mix(aStart,aEnd,t);vec4 mv=modelViewMatrix*vec4(p,1.);gl_Position=projectionMatrix*mv;gl_PointSize=(2.2+uResonance*2.8)*uPixelRatio;vPower=.72+.28*sin(t*3.14159265);}`;const FLOW_FRAGMENT = `varying float vPower;void main(){vec2 p=abs(gl_PointCoord-.5);float d=p.x+p.y;if(d>.5)discard;float e=1.0-smoothstep(.38,.5,d);gl_FragColor=vec4(vec3(.62,.94,1.)*vPower,e*vPower);}`;function centeredPoint(random, extent, exclusion) {  const point = new THREE.Vector3();  for (let attempt=0; attempt<32; attempt++) {    for (let axis=0; axis<3; axis++) {      const sign=random()<.5?-1:1;      point.setComponent(axis,sign*Math.pow(random(),1.65)*extent[axis]*.5);    }    const e=(point.x/exclusion[0])**2+(point.y/exclusion[1])**2+(point.z/exclusion[2])**2;    if(e>=1) return point;  }  point.x=Math.sign(point.x||1)*exclusion[0];return point;}function clearsExclusion(a,b,exclusion) {  for(let step=1;step<8;step++){    const t=step/8,x=THREE.MathUtils.lerp(a.x,b.x,t)/exclusion[0];    const y=THREE.MathUtils.lerp(a.y,b.y,t)/exclusion[1],z=THREE.MathUtils.lerp(a.z,b.z,t)/exclusion[2];    if(x*x+y*y+z*z<1) return false;  }  return true;}export function createSharpCosmicWeb(options = {}) {  const random=createRandom(options.seed??8808),nodeCapacity=Math.max(12,options.nodes??72);  const segmentCapacity=Math.min(options.capacity??MAX_ENVIRONMENT_BUDGET.webSegments,MAX_ENVIRONMENT_BUDGET.webSegments);  const extent=options.extent??[440,260,360],exclusion=options.exclusion??extent.map(value=>value*.12);  const nodes=Array.from({length:nodeCapacity},()=>centeredPoint(random,extent,exclusion));  const candidates=[];  for(let a=0;a<nodeCapacity;a++)for(let b=a+1;b<nodeCapacity;b++)if(clearsExclusion(nodes[a],nodes[b],exclusion))    candidates.push([a,b,nodes[a].distanceToSquared(nodes[b])]);  candidates.sort((a,b)=>a[2]-b[2]);const pairs=candidates.slice(0,segmentCapacity);  const vertexCount=pairs.length*4,positions=new Float32Array(vertexCount*3),starts=new Float32Array(vertexCount*3);  const ends=new Float32Array(vertexCount*3),sides=new Float32Array(vertexCount),along=new Float32Array(vertexCount);  const paths=new Float32Array(vertexCount),widths=new Float32Array(vertexCount),indices=new Uint16Array(pairs.length*6);  pairs.forEach(([a,b],i)=>{const width=(options.ribbonWidth??1.15)*(.72+random()*.65);    for(let v=0;v<4;v++){const n=i*4+v,n3=n*3;starts.set(nodes[a].toArray(),n3);ends.set(nodes[b].toArray(),n3);      sides[n]=v%2?-1:1;along[n]=v<2?0:1;paths[n]=(i+random()*.2)/Math.max(1,pairs.length);widths[n]=width;}    indices.set([i*4,i*4+1,i*4+2,i*4+2,i*4+1,i*4+3],i*6);});  const ribbonGeometry=new THREE.BufferGeometry();  ribbonGeometry.setAttribute('position',new THREE.BufferAttribute(positions,3));ribbonGeometry.setAttribute('aStart',new THREE.BufferAttribute(starts,3));  ribbonGeometry.setAttribute('aEnd',new THREE.BufferAttribute(ends,3));ribbonGeometry.setAttribute('aSide',new THREE.BufferAttribute(sides,1));  ribbonGeometry.setAttribute('aAlong',new THREE.BufferAttribute(along,1));ribbonGeometry.setAttribute('aPath',new THREE.BufferAttribute(paths,1));  ribbonGeometry.setAttribute('aWidth',new THREE.BufferAttribute(widths,1));ribbonGeometry.setIndex(new THREE.BufferAttribute(indices,1));  const uniforms={uTime:{value:0},uBreath:{value:1},uResonance:{value:0},uOpacity:{value:options.opacity??.72},    uPixelRatio:{value:options.pixelRatio??1},uViewport:{value:new THREE.Vector2(1920,1080)}};  const ribbons=new THREE.Mesh(ribbonGeometry,new THREE.ShaderMaterial({uniforms,vertexShader:RIBBON_VERTEX,fragmentShader:RIBBON_FRAGMENT,    transparent:true,depthWrite:false,side:THREE.DoubleSide,blending:THREE.AdditiveBlending,extensions:{derivatives:true}}));ribbons.frustumCulled=false;  const nodeGeometry=new THREE.OctahedronGeometry(options.nodeSize??2.4,0);  const nodeMaterial=new THREE.MeshBasicMaterial({color:0xffffff,vertexColors:true,transparent:true,opacity:.7,blending:THREE.AdditiveBlending});  const crystals=new THREE.InstancedMesh(nodeGeometry,nodeMaterial,nodeCapacity),matrix=new THREE.Matrix4();  const nodeColor=new THREE.Color();  nodes.forEach((p,i)=>{matrix.compose(p,new THREE.Quaternion().setFromEuler(new THREE.Euler(random()*3,random()*3,random()*3)),    new THREE.Vector3().setScalar(.45+random()*1.1));crystals.setMatrixAt(i,matrix);    nodeColor.set(i%5===0?0xe779bd:i%3===0?0x75e5ef:0x9ac7e8);crystals.setColorAt(i,nodeColor);});  crystals.instanceMatrix.needsUpdate=true;crystals.instanceColor.needsUpdate=true;  const flowCapacity=Math.min(options.flowPoints??96,pairs.length),flowGeometry=new THREE.BufferGeometry();  const flowStarts=new Float32Array(flowCapacity*3),flowEnds=new Float32Array(flowCapacity*3),phases=new Float32Array(flowCapacity);  for(let i=0;i<flowCapacity;i++){const pair=pairs[(i*7)%pairs.length];flowStarts.set(nodes[pair[0]].toArray(),i*3);flowEnds.set(nodes[pair[1]].toArray(),i*3);phases[i]=random();}  flowGeometry.setAttribute('position',new THREE.BufferAttribute(new Float32Array(flowCapacity*3),3));flowGeometry.setAttribute('aStart',new THREE.BufferAttribute(flowStarts,3));  flowGeometry.setAttribute('aEnd',new THREE.BufferAttribute(flowEnds,3));flowGeometry.setAttribute('aPhase',new THREE.BufferAttribute(phases,1));  const flow=new THREE.Points(flowGeometry,new THREE.ShaderMaterial({uniforms,vertexShader:FLOW_VERTEX,fragmentShader:FLOW_FRAGMENT,    transparent:true,depthWrite:false,blending:THREE.AdditiveBlending}));flow.frustumCulled=false;  const object3D=new THREE.Group();object3D.add(ribbons,crystals,flow);setPosition(object3D,options.position,[0,0,-500]);  const api={object3D,lines:ribbons,ribbons,crystals,flow,capacity:segmentCapacity,drawCalls:3,    update(delta=0,elapsed){uniforms.uTime.value=Number.isFinite(elapsed)?elapsed:uniforms.uTime.value+delta;crystals.rotation.y+=delta*.006*(1+uniforms.uResonance.value);},    setQuality(value='high',mobile=false){const budget=getQualityBudget(value,mobile),segments=Math.min(pairs.length,budget.webSegments);      ribbonGeometry.setDrawRange(0,segments*6);flowGeometry.setDrawRange(0,Math.min(flowCapacity,budget.webFlowPoints));crystals.count=Math.min(nodeCapacity,Math.max(12,Math.ceil(nodeCapacity*segments/Math.max(1,pairs.length))));},    setPixelRatio(value=1){uniforms.uPixelRatio.value=THREE.MathUtils.clamp(value||1,.5,2);},    setViewport(width=1920,height=1080){uniforms.uViewport.value.set(Math.max(1,width)*uniforms.uPixelRatio.value,Math.max(1,height)*uniforms.uPixelRatio.value);},    setResonance(value=0){uniforms.uResonance.value=THREE.MathUtils.clamp(value,0,1);},setBreathing(enabled=true){uniforms.uBreath.value=enabled?1:0;},    setOpacity(value=1){uniforms.uOpacity.value=THREE.MathUtils.clamp(value,0,1);},dispose(){disposeRenderable(ribbons);disposeRenderable(crystals);disposeRenderable(flow);object3D.removeFromParent();object3D.clear();}};  api.setQuality(options.quality,options.mobile);return api;}