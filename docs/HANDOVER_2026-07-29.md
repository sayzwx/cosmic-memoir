# CosmicMemoir 项目交接文档

> 文档版本：2026-07-29 电影级 M8 空间记忆版  
> 源码仓库：https://github.com/sayzwx/cosmic-memoir  
> 在线页面：https://sayzwx.github.io/cosmic-memoir/  
> 当前源码基线：`16a2bbb feat: rebuild dark matter as spatial memoir`

## 1. 当前状态

### 登录页

- 状态：已完成并冻结。
- 技术：React 18、React Three Fiber、Three.js、后处理和 DOM 登录 UI。
- 保留当前视觉、交互和性能限制；除非明确重新开启，不再优化登录页。
- 桌面 WebGL DPR 上限为 1.25，登录银河 Canvas DPR 上限为 1.25。
- `GalaxyRiver` 保持 30fps、720 粒子，不得恢复到 60fps 或大幅增加粒子。

### 主页面

- 入口：`universe.html`。
- 当前阶段：按模块逐章重构。
- M2 叙事 UI：规格已冻结，见 `docs/M2_NARRATIVE_UI_SPEC.md`。
- M8 暗物质章节：电影级空间记忆 v2 已完成第一版实现。
- M9 红移、M10 事件视界、M11 洛希极限、M12 引力波：仍为原实现，后续逐章规划优化。

## 2. 账号与认证

- Username：`mjsx`
- Password：`foo`
- React 登录逻辑：`src/hooks/useAuth.js`
- 主页面路由守卫：`js/auth-guard.js`
- 会话有效期：1 小时。
- 清除登录状态：访问 `?logout`。

注意：这是 GitHub Pages 静态网站。认证逻辑、JSON 和照片资源均可被技术用户直接读取，不适合保存真正敏感或私密内容。

## 3. M8 电影级空间记忆

### 场景环境

M8 不再使用大面积信纸、信封和照片 DOM 覆盖层。照片已经成为 Three.js 空间中的 3D 实体。

- 三层温差星场：蓝白主序星、暖金巨星和冷色远星。
- 倾斜旋涡星系：四条旋臂、棒状核心、粉紫 HII 区和缓慢自转。
- 近景大气行星：程序化表面、动态云层和 Rayleigh 风格边缘光。
- 暗紫、靛蓝和金红色星云尘埃。
- 暗物质宇宙网：批量 `LineSegments`，随探索进度显形。
- 照片透镜：细分平面 UV 弯曲；High 档启用 RGB 色散和加法辉光。

### 五张空间照片

| ID | 角色 | 标题 | 发现机制 |
|---|---|---|---|
| `m8-origin` | `cover` | 最初的轨道 | 聚焦后凝视 |
| `m8-window` | `letter` | 晚一分钟的光 | 爱因斯坦环接近与对准 |
| `m8-summer` | `hiddenMemory` | 未记录的夏天 | 必须 Space 或移动端扫描按钮 |
| `m8-reflection` | `lensReflection` | 透镜中的我们 | 强透镜聚焦和色散 |
| `m8-dawn` | `epilogue` | 黎明后的质量 | 前四张完成后解锁 |

五张照片按 `unlockAfter` 数据依赖顺序解锁。全部访问后进入 `TABLEAU`：

- 五张照片重新排列为星座构图。
- 暗物质宇宙网完全显形。
- 解锁隐藏记忆 `mem_1998_humidity`。
- 隐藏正文显示在空间叙事区域。

### 相机和输入

相机状态：`INTRO`、`OVERVIEW`、`ORBITING`、`FOCUSING`、`FOCUSED`、`RETURNING`、`TABLEAU`。

桌面端：

- 鼠标拖动：环绕视角。
- 滚轮：深潜缩放。
- `W/S` 或 `↑/↓`：前进、后退。
- `A/D` 或 `←/→`：左右环绕。
- `Enter`：聚焦当前可用记忆。
- `Space`：扫描当前记忆。
- `Esc`：返回总览。

移动端：

- 单指拖动：环绕。
- 双指捏合：缩放。
- 空间 UI 扫描按钮：扫描第三张暗网记忆。
- 双指开始时会取消单指拖动，避免误捕获或状态残留。
- 陀螺仪尚未默认开启，避免未经授权请求传感器权限。

## 4. M8 性能预算

典型 High 画质约 13 个 draw calls：环境 7、五张照片 5、爱因斯坦环 1。

| 档位 | 星场 | 星云尘埃 | 星系粒子 | 宇宙网线段 | DPR |
|---|---:|---:|---:|---:|---:|
| High | 18,000 | 5,000 | 9,000 | 900 | 1.5 |
| Medium | 10,000 | 2,500 | 5,600 | 560 | 1.25 |
| Low | 4,000 | 800 | 2,600 | 260 | 1.0 |
| Mobile cap | 6,000 | 1,200 | 4,200 | 360 | 1.0 |

性能原则：

- 不默认堆到 50,000 个大透明粒子；填充率比顶点数更容易成为瓶颈。
- 移动端按移动预算创建 Buffer，不预分配桌面 High 容量。
- Low 档关闭行星云层和照片色散。
- 高频遥测最多 15Hz，且避免重复写入相同的 `aria-live` 文本。
- 不使用全屏 Ray Marching 或重型多 Pass 后处理。

## 5. M8 模块结构

```text
engine/renderers/
├── DarkMatterRenderer.js
└── darkMatter/
    ├── AtmosphericPlanet.js
    ├── CosmicWeb.js
    ├── DarkMatterEnvironment.js
    ├── DeepSpaceField.js
    ├── M8SpatialPhotoShaders.js
    ├── NebulaDust.js
    ├── SpatialMemoryCamera.js
    ├── SpatialPhotoEntity.js
    ├── SpiralGalaxy.js
    ├── index.js
    ├── math.js
    └── qualityBudgets.js
```

职责：

| 文件 | 职责 |
|---|---|
| `DarkMatterRenderer.js` | 场景装配、五实体解锁、输入、遥测和完成流程 |
| `DarkMatterEnvironment.js` | 组合星场、星系、行星、尘埃和宇宙网 |
| `SpatialPhotoEntity.js` | 照片纹理、Shader、状态、命中和资源释放 |
| `SpatialMemoryCamera.js` | 电影式入场、环绕、聚焦、返回和终章构图 |
| `qualityBudgets.js` | High/Medium/Low/Mobile 资源预算 |

## 6. 数据契约

M8 由 `data/memories.json` 的显式版本字段识别：

```json
{
  "experience": {
    "id": "M8",
    "variant": "deepSpaceSpatialMemory",
    "version": 2,
    "entityOrder": []
  }
}
```

每张空间照片必须包含：

- `id`
- `role`
- `src`
- `alt`
- `caption`
- `title`
- `body`
- `position[3]`
- `rotation[3]`
- `size[2]`
- `focusOffset[3]`
- `unlockAfter[]`
- `lensingStrength`
- `accent`
- `discovery.type`

`DataLoader.validateSpatialMemoryV2()` 会在挂载前检查五实体数量、唯一 ID、向量格式、依赖引用和依赖环。

当前照片是本地轻量占位素材：

```text
assets/memories/m8/
```

后续替换真实照片时，保持字段和路径契约即可，不需要修改 Renderer。建议使用 WebP/AVIF、最长边 1600px 以内、单张约 200KB，并清除 EXIF/GPS。

## 7. 跨模块事件

Renderer 输出：

- `sceneTelemetry`
- `spatialMemoryFocus`
- `spatialMemoryVisited`
- `darkMatterReady`
- `darkMatterTelemetry`（兼容事件，仅离散状态强制发送）
- `darkMatterMilestone`
- `hiddenMemoryUnlocked`

UI 输入：

- `spatialMemoryScanRequested`
- `spatialMemoryReturnOverview`

M8 事件应携带 `memoryId`，避免场景快速切换时旧事件污染当前 UI。

## 8. 共享引擎改动

- `SceneRouter` 增加 mount generation，过期或已销毁的异步场景不会重新挂载。
- 场景初始化失败时释放尚未提交的 Renderer。
- `CosmicMemoirApp` 增加导航事务锁，防止连续点击跳章或越界。
- 当前章节和索引在 `sceneMounted` 前提交，保证进度 UI 使用正确状态。
- `InputAdapter` 增加 pinch `start/move/end` 生命周期。
- 非 M8 场景仍保留 `Space` 前进快捷键。

## 9. 关键文件

| 文件 | 用途 |
|---|---|
| `universe.html` | 主页面 Canvas、共享 UI 和轻量 M8 空间 Presenter |
| `css/universe-ui.css` | M8 空间准星、Caption、进度和移动安全区 |
| `js/app.js` | 主页面 UI、事件桥接和隐藏记忆显示 |
| `engine/App.js` | 章节、记忆、导航事务和质量配置 |
| `engine/core/SceneRouter.js` | 动态场景挂载、取消和转场 |
| `engine/core/InputAdapter.js` | 鼠标、触摸、滚轮和 pinch 归一化 |
| `engine/core/DataLoader.js` | 数据加载、图片预载和 M8 v2 校验 |
| `data/memories.json` | 内容、物理参数和空间照片配置 |
| `docs/MAIN_PAGE_MODULES.md` | 主页面模块地图 |
| `docs/M2_NARRATIVE_UI_SPEC.md` | M2 实施规格 |

## 10. 构建、测试和发布

```powershell
npm run dev
npm test
npm run build
npx gh-pages -d dist -b gh-pages -m "deploy: message"
```

实际 GitHub Pages 来源：

- 模式：Deploy from a branch
- 分支：`gh-pages`
- 目录：`/(root)`

`.github/workflows/deploy.yml` 当前不是线上 Pages 的实际来源。

当前验证：

- `npm test`：93/93 passed。
- `npm run build`：passed。
- 所有新增 M8 模块 `node --check`：passed。
- `git diff --check`：passed。
- 构建仍有登录 React 主包约 1.1MB 的既有警告。

## 11. 自动同步

- Windows 计划任务：`CosmicMemoir-AutoSync`。
- 脚本：`scripts/auto-sync.ps1`。
- 每 30 分钟检测项目变更并推送 `main`。
- 自动同步只处理 `main`；线上更新仍需发布 `gh-pages`。

## 12. 后续工作

建议顺序：

1. 使用真实照片替换 M8 本地占位 SVG，并做视觉验收。
2. 在已登录浏览器中完成 M8 桌面和移动端完整 E2E。
3. 对 M8 增加专门的 Renderer/SceneRouter 自动化测试。
4. 完成 M2 叙事 UI Phase 1。
5. 依次规划 M9 红移、M10 事件视界、M11 洛希极限和 M12 引力波。
6. 后续统一主页面 Three.js CDN r160 与登录页 npm Three.js r170 的版本。

## 13. 注意事项

- 不要提交 `dist` 或 `node_modules`。
- 不要把 M8 恢复成拖动照片/星系拼图；当前体验是空间相机探索。
- 不要给每颗星、每根丝线创建独立 Mesh。
- 不要默认打开 50,000 个大透明粒子或重型全屏多 Pass。
- M8 环境和照片由模块自行 dispose，不要重复加入基类 `disposables`。
- 保持非 M8 Renderer 的输入签名兼容。
- GitHub Pages 中的隐藏内容只是视觉隐藏，不是真正访问控制。
